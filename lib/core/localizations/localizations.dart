import 'package:flutter/material.dart';

const List<Locale> supportedLanguage = [Locale('vi'), Locale('en')];
const Locale defaultLanguage = Locale('vi');

const Map<String, String> languageMap = {
  'vi': 'Tiếng Việt',
  'en': 'English',
};
